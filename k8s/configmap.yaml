apiVersion: v1
kind: ConfigMap
metadata:
  name: fake-news-verifier-config
  labels:
    app: fake-news-verifier
data:
  # Mock / config values (override per env)
  LOG_LEVEL: "INFO"
  DATA_PATH: "/app/data"
