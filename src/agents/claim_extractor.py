"""
Claim extractor agent — extracts claims from input (e.g. articles).
"""


def extract_claims(text: str) -> list[dict]:
    """Extract claims from text. Returns list of claim dicts (stub)."""
    # TODO: implement extraction (e.g. NER, rule-based, or model)
    return []
